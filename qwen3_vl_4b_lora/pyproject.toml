[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "qwen3-vl-fmcg-lora"
version = "0.2.0"
description = "Multi-agent FMCG dataset curation and Qwen3-VL QLoRA training bundle"
requires-python = ">=3.11"
dependencies = []
